// src/world/Mapping.js

/**
 * This file contains the layout for all static models in the world.
 * Each object in the array represents a model to be loaded.
 *
 * Properties:
 * - name: A unique identifier for the model.
 * - path: The path to the .glb or .gltf file.
 * - position: An object { x, y, z } for the model's location.
 * - scale: An object { x, y, z } for the model's scale.
 * - rotation: An object { x, y, z } for the model's rotation in radians.
 */

export const worldObjects = [
    // Example of how an object would look:
    /*
    {
        name: "MainBaseStructure",
        path: "src/assets/models/base.glb",
        position: { x: 0, y: 0.5, z: -20 },
        scale: { x: 1, y: 1, z: 1 },
        rotation: { x: 0, y: Math.PI / 2, z: 0 }
    }
    */
];
